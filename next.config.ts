import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Production optimizations
  // Note: We're using a custom server.js for Railway port configuration
  // If you switch to standalone output, you can remove server.js
};

export default nextConfig;
